---
title: "Conventions"
description: "Formatting, comments, and file naming rules."
---

## File conventions

- One provider per file: `config/providers/<provider>.conf` (see [config/providers](https://github.com/r9s-ai/open-next-router/tree/main/config/providers))
- File name must match provider name (case-insensitive match; normalized to lowercase)

## Syntax conventions

- Semicolon `;` is required for every statement
- Only blocks use `{}`; directives do not
- Recommended: one directive per line (better diffs)

Comments are supported: `#`, `//`, `/* ... */`.
