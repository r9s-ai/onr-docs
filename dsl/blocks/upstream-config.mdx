---
title: "upstream_config"
description: "Declare upstream base URL."
---

```conf
upstream_config {
  base_url = "https://api.example.com";
}
```

- `base_url` is required and must be a string literal.
- Runtime channel/provider key can override base URL when configured.

