# onr-docs

Mintlify documentation site for Open Next Router.

## Local development

```bash
cd onr-docs
npx mintlify dev
```

The site reads `docs.json` as the navigation and site config entry point.
